/**
 * HTTP endpoints and form handling.
 *
 * <p>A controller maps a request to a service call and picks the Thymeleaf
 * template that renders the result. It holds no business rules and — rule one of
 * the layering — never touches a repository directly.
 */
package com.library.controller;
