/**
 * Domain classes: {@code Book}, {@code Member}, {@code Loan}, {@code Reservation}.
 *
 * <p>Plain data objects — fields, constructors, getters, {@code toString()}. No
 * SQL, no validation, no dependency on any other layer.
 */
package com.library.domain;
